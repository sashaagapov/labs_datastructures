export const codes = {
  bstSearch: [
    "public RecursiveTree Search(RecursiveTree node, int value)",
    "{",
    "    if (node == null || node.Value == value)",
    "        return node;",
    "    if (value < node.Value)",
    "        return Search(node.Left, value);",
    "    return Search(node.Right, value);",
    "}"
  ],
  leftRotate: [
    "public RecursiveTree LeftRotate(RecursiveTree root, RecursiveTree x)",
    "{",
    "    RecursiveTree y = x.Right;",
    "    RecursiveTree B = y.Left;",
    "    x.SetRight(B);",
    "    y.Parent = x.Parent;",
    "    if (x.Parent == null) root = y; else if (x.Parent.Left == x) x.Parent.SetLeft(y); else x.Parent.SetRight(y);",
    "    y.SetLeft(x);",
    "    return root;",
    "}"
  ],
  rightRotate: [
    "public RecursiveTree RightRotate(RecursiveTree root, RecursiveTree y)",
    "{",
    "    RecursiveTree x = y.Left;",
    "    RecursiveTree B = x.Right;",
    "    y.SetLeft(B);",
    "    x.Parent = y.Parent;",
    "    if (y.Parent == null) root = x; else if (y.Parent.Left == y) y.Parent.SetLeft(x); else y.Parent.SetRight(x);",
    "    x.SetRight(y);",
    "    return root;",
    "}"
  ],
  bstDelete: [
    "public RecursiveTree TreeDelete(RecursiveTree root, RecursiveTree z)",
    "{",
    "    if (z.Left == null)",
    "        Transplant(root, z, z.Right);",
    "    else if (z.Right == null)",
    "        Transplant(root, z, z.Left);",
    "    else",
    "    {",
    "        RecursiveTree y = TreeMinimum(z.Right);",
    "        if (y.Parent != z)",
    "        {",
    "            Transplant(root, y, y.Right);",
    "            y.SetRight(z.Right);",
    "        }",
    "        Transplant(root, z, y);",
    "        y.SetLeft(z.Left);",
    "    }",
    "    return root;",
    "}"
  ],
  transplant: [
    "public RecursiveTree Transplant(RecursiveTree root, RecursiveTree u, RecursiveTree v)",
    "{",
    "    if (u.Parent == null)",
    "        root = v;",
    "    else if (u == u.Parent.Left)",
    "        u.Parent.SetLeft(v);",
    "    else",
    "        u.Parent.SetRight(v);",
    "    if (v != null)",
    "        v.Parent = u.Parent;",
    "    return root;",
    "}"
  ],
  avlInsert: [
    "public Node Insert(Node node, int value)",
    "{",
    "    node = BstInsert(node, value);",
    "    UpdateHeight(node);",
    "    int balance = GetBalanceFactor(node);",
    "    if (balance > 1 && value < node.Left.Value)",
    "        return RightRotate(node);     // LL",
    "    if (balance < -1 && value > node.Right.Value)",
    "        return LeftRotate(node);      // RR",
    "    if (balance > 1 && value > node.Left.Value)",
    "    {",
    "        node.Left = LeftRotate(node.Left);",
    "        return RightRotate(node);     // LR",
    "    }",
    "    if (balance < -1 && value < node.Right.Value)",
    "    {",
    "        node.Right = RightRotate(node.Right);",
    "        return LeftRotate(node);      // RL",
    "    }",
    "    return node;",
    "}",
    "",
    "private Node LeftRotate(Node x)",
    "{",
    "    Node y = x.Right;",
    "    Node B = y.Left;",
    "    x.SetRight(B);",
    "    y.Parent = x.Parent;",
    "    if (x.Parent != null) { if (x.Parent.Left == x) x.Parent.SetLeft(y); else x.Parent.SetRight(y); }",
    "    y.SetLeft(x);",
    "    return y;",
    "}",
    "",
    "private Node RightRotate(Node y)",
    "{",
    "    Node x = y.Left;",
    "    Node B = x.Right;",
    "    y.SetLeft(B);",
    "    x.Parent = y.Parent;",
    "    if (y.Parent != null) { if (y.Parent.Left == y) y.Parent.SetLeft(x); else y.Parent.SetRight(x); }",
    "    x.SetRight(y);",
    "    return x;",
    "}"
  ],
  avlDelete: [
    "public Node Delete(Node root, int value)",
    "{",
    "    root = BstDelete(root, value);",
    "    Node current = ParentOfDeletedPosition;",
    "    while (current != null)",
    "    {",
    "        UpdateHeight(current);",
    "        int bf = GetBalanceFactor(current);",
    "        if (bf > 1)",
    "        {",
    "            int childBf = GetBalanceFactor(current.Left);",
    "            if (childBf >= 0) current = RightRotate(current);    // LL after delete",
    "            else { current.Left = LeftRotate(current.Left); current = RightRotate(current); } // LR after delete",
    "        }",
    "        else if (bf < -1)",
    "        {",
    "            int childBf = GetBalanceFactor(current.Right);",
    "            if (childBf <= 0) current = LeftRotate(current);    // RR after delete",
    "            else { current.Right = RightRotate(current.Right); current = LeftRotate(current); } // RL after delete",
    "        }",
    "        current = current.Parent;",
    "    }",
    "    return root;",
    "}"
  ],
  rbInsert: [
    "public Node RbInsert(Node root, int key)",
    "{",
    "    Node z = BstInsert(root, key);",
    "    z.Color = Red;",
    "",
    "    while (z.Parent != null && z.Parent.Color == Red)",
    "    {",
    "        Node p = z.Parent;",
    "        Node g = p.Parent;",
    "        if (p == g.Left)",
    "        {",
    "            Node u = g.Right; // uncle",
    "            if (u != null && u.Color == Red)",
    "            {",
    "                p.Color = Black; u.Color = Black; g.Color = Red;",
    "                z = g; // підіймаємось вище",
    "            }",
    "            else",
    "            {",
    "                if (z == p.Right) { z = p; LeftRotate(ref root, z); }",
    "                z.Parent.Color = Black;",
    "                z.Parent.Parent.Color = Red;",
    "                RightRotate(ref root, z.Parent.Parent);",
    "            }",
    "        }",
    "        else",
    "        {",
    "            Node u = g.Left; // дзеркально",
    "            if (u != null && u.Color == Red)",
    "            {",
    "                p.Color = Black; u.Color = Black; g.Color = Red;",
    "                z = g;",
    "            }",
    "            else",
    "            {",
    "                if (z == p.Left) { z = p; RightRotate(ref root, z); }",
    "                z.Parent.Color = Black;",
    "                z.Parent.Parent.Color = Red;",
    "                LeftRotate(ref root, z.Parent.Parent);",
    "            }",
    "        }",
    "    }",
    "",
    "    root.Color = Black;",
    "    return root;",
    "}"
  ]
};
